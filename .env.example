# Example environment variables for local development
VITE_SHOW_DASHBOARD=true
# NEXTCLOUD_URL, NEXTCLOUD_USER and NEXTCLOUD_PASS are written to server/.env.local via setup_local_env.sh
# Do NOT commit server/.env.local with secrets
