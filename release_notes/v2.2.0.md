# v2.2.0 — Accessibility & OTA Update Release

## ✨ Highlights
- Accessibility fixes in `MainDashboard.vue` (caption, scope, aria-label, contrast, focus outlines)
- OTA update mechanism via Service Worker and `SwUpdateToast.vue`
- GitHub Pages demo deployment (see README for link)
- CI workflow improvements (resilient monitor, SW verification, dry-run validation)

## 🔧 Technical
- `monitor_a11y_ci.py` CLI patched for `--dry-run` and `--verbose` flags
- `accessibility-diff.yml` hardened with debug and verify steps
- `deploy.yml` added for GitHub Pages auto-deploy

## Notes
- The release tag `v2.2.0` was created and pushed.
- Enable GitHub Pages in the repository settings (Pages → Source: `gh-pages` branch, Folder: `/`).

